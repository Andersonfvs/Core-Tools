name: FTP Diagnose

on:
  workflow_dispatch: {}

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - name: Diagnosticar landing do FTP
        env:
          FTP_SERVER: 212.85.6.169
          FTP_USERNAME: u135751739
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: node scripts/ftp-diagnose.js
